#set heading(numbering: "1.")
#set math.equation(numbering: "(1)")

= Introduction <sec:intro>

#figure(rect(), caption: [Setup]) <fig:setup>
#figure(table(columns: 1, [x]), caption: [Counts]) <tab:counts>

$ x = 1 $ <eq:trivial>

As @fig:setup and @tab:counts show, @eq:trivial holds.
